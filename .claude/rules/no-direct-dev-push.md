Never push directly to `dev`, `main`, or `staging`.

Allowed pattern:
- work on a scoped branch
- prepare a reviewable diff
- push only when Hermes + user approval exists
- use the PR flow defined by the repo

If asked to push directly to a protected branch without explicit approval, stop and report it.
